# System Requirements
1. The platform must analyze market trends using AI.
2. The platform must be built with a .NET backend and React frontend.
3. The database must be PostgreSQL.
4. The ML pipeline must utilize Python and PyTorch.
5. Inference must be achievable on multiple platforms using ONNX Runtime.
